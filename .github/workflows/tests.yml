name: Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive

#      - name: Install test node dependencies
#        run: |
#          sudo apt-get update && sudo apt-get install -y \
#            cmake pkg-config libssl-dev clang

#      - name: Debug info
#        run: |
#          sudo find / -name 'libssl*'
#
#      - name: Link libssl
#        run: |
#          sudo ln -s /usr/lib/x86_64-linux-gnu/libssl.so.1.1 /lib/x86_64-linux-gnu/libssl.so.1.0.0 && \
#          sudo ln -s /usr/lib/x86_64-linux-gnu/libcrypto.so.1.1 /lib/x86_64-linux-gnu/libcrypto.so.1.0.0

      - name: Install test node
        run: |
          sudo apt-get install unzip &&
          curl -o node.zip https://github.com/matter-labs/era-test-node/suites/15355900259/artifacts/876886264
          unzip node.zip
          tar -xzf era_test_node-x86_64-unknown-linux-musl.tar.gz.zip
          chmod +x era_test_node

      - name: Run test node
        run: |
          ./era_test_node run &

      - uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - id: yarn-cache
        run: echo "::set-output name=dir::$(yarn cache dir)"

      - uses: actions/cache@v1
        with:
          path: ${{ steps.yarn-cache.outputs.dir }}
          key: yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            yarn-
      
      - name: Install dependencies
        run: yarn install --frozen-lockfile
        
      # This is required separately from yarn test because it generates the typechain definitions
      - name: Compile
        run: yarn compile

      - name: Run tests
        run: yarn test
